-r requirements.txt

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0          # coverage integration
pytest-xdist>=3.5.0        # parallel test execution: pytest -n auto
pytest-asyncio>=0.24.0     # required for asyncio_mode / asyncio_default_fixture_loop_scope config

# Linting & formatting
ruff>=0.4.0                # linter + formatter (replaces black + isort)
black>=24.0.0              # formatter fallback / IDE integration
mypy>=1.10.0               # static type checking

# Security scanning
bandit>=1.7.9              # SAST — OWASP top-10 checks
safety>=3.2.0              # dependency CVE scanner
